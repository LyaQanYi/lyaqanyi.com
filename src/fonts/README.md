# 标题字体

`cmdysj.woff2` 由用户提供的 `cmdysj.ttf` 转换而来，保留完整字形，未做字符裁剪。

- 原始文件：8,470,944 字节。
- 网页文件：3,861,320 字节。
- 转换校验：30,786 个字符映射及字形顺序一致，轮廓、字宽和排版表一致。

共享加载配置在 `src/lib/fonts.ts`。全站通过 `--f-display` 使用该字体，正文和导航沿用 `--f-sans`，辅助信息沿用 `--f-mono`。

原版 Instrument Serif 仅在 `/font-preview` 对比页加载。
